Config = {
    bootstrap.servers=localhost:9101
    acks=all
    batch.size=100
    client.id=mirror_maker_producer
    sasl.jaas.config=./config/zookeeper.sasl.jaas.conf
    zookeeper.connect=localhost:2182
    zookeeper.connection.timeout.ms=6000
};